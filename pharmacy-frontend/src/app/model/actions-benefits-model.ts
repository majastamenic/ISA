export interface ActionsBenefits{
    id: number;
    message: string;
    startDate: Date;
    endDate: Date;
}

export interface ActionsBenefitsDto{
    message: string;
    startDate: Date;
    endDate: Date;
}